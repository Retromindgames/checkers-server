// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Lavizord/checkers-server/postgrescli/ent/gameconfig"
	"github.com/Lavizord/checkers-server/postgrescli/ent/mathversion"
	"github.com/Lavizord/checkers-server/postgrescli/ent/predicate"
	"github.com/Lavizord/checkers-server/postgrescli/ent/session"
)

// MathVersionQuery is the builder for querying MathVersion entities.
type MathVersionQuery struct {
	config
	ctx             *QueryContext
	order           []mathversion.OrderOption
	inters          []Interceptor
	predicates      []predicate.MathVersion
	withSessions    *SessionQuery
	withGameConfigs *GameConfigQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the MathVersionQuery builder.
func (mvq *MathVersionQuery) Where(ps ...predicate.MathVersion) *MathVersionQuery {
	mvq.predicates = append(mvq.predicates, ps...)
	return mvq
}

// Limit the number of records to be returned by this query.
func (mvq *MathVersionQuery) Limit(limit int) *MathVersionQuery {
	mvq.ctx.Limit = &limit
	return mvq
}

// Offset to start from.
func (mvq *MathVersionQuery) Offset(offset int) *MathVersionQuery {
	mvq.ctx.Offset = &offset
	return mvq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (mvq *MathVersionQuery) Unique(unique bool) *MathVersionQuery {
	mvq.ctx.Unique = &unique
	return mvq
}

// Order specifies how the records should be ordered.
func (mvq *MathVersionQuery) Order(o ...mathversion.OrderOption) *MathVersionQuery {
	mvq.order = append(mvq.order, o...)
	return mvq
}

// QuerySessions chains the current query on the "sessions" edge.
func (mvq *MathVersionQuery) QuerySessions() *SessionQuery {
	query := (&SessionClient{config: mvq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mvq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mvq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mathversion.Table, mathversion.FieldID, selector),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, mathversion.SessionsTable, mathversion.SessionsColumn),
		)
		fromU = sqlgraph.SetNeighbors(mvq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryGameConfigs chains the current query on the "game_configs" edge.
func (mvq *MathVersionQuery) QueryGameConfigs() *GameConfigQuery {
	query := (&GameConfigClient{config: mvq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := mvq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := mvq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(mathversion.Table, mathversion.FieldID, selector),
			sqlgraph.To(gameconfig.Table, gameconfig.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, mathversion.GameConfigsTable, mathversion.GameConfigsColumn),
		)
		fromU = sqlgraph.SetNeighbors(mvq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first MathVersion entity from the query.
// Returns a *NotFoundError when no MathVersion was found.
func (mvq *MathVersionQuery) First(ctx context.Context) (*MathVersion, error) {
	nodes, err := mvq.Limit(1).All(setContextOp(ctx, mvq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{mathversion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (mvq *MathVersionQuery) FirstX(ctx context.Context) *MathVersion {
	node, err := mvq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first MathVersion ID from the query.
// Returns a *NotFoundError when no MathVersion ID was found.
func (mvq *MathVersionQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mvq.Limit(1).IDs(setContextOp(ctx, mvq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{mathversion.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (mvq *MathVersionQuery) FirstIDX(ctx context.Context) int {
	id, err := mvq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single MathVersion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one MathVersion entity is found.
// Returns a *NotFoundError when no MathVersion entities are found.
func (mvq *MathVersionQuery) Only(ctx context.Context) (*MathVersion, error) {
	nodes, err := mvq.Limit(2).All(setContextOp(ctx, mvq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{mathversion.Label}
	default:
		return nil, &NotSingularError{mathversion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (mvq *MathVersionQuery) OnlyX(ctx context.Context) *MathVersion {
	node, err := mvq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only MathVersion ID in the query.
// Returns a *NotSingularError when more than one MathVersion ID is found.
// Returns a *NotFoundError when no entities are found.
func (mvq *MathVersionQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = mvq.Limit(2).IDs(setContextOp(ctx, mvq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{mathversion.Label}
	default:
		err = &NotSingularError{mathversion.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (mvq *MathVersionQuery) OnlyIDX(ctx context.Context) int {
	id, err := mvq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of MathVersions.
func (mvq *MathVersionQuery) All(ctx context.Context) ([]*MathVersion, error) {
	ctx = setContextOp(ctx, mvq.ctx, ent.OpQueryAll)
	if err := mvq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*MathVersion, *MathVersionQuery]()
	return withInterceptors[[]*MathVersion](ctx, mvq, qr, mvq.inters)
}

// AllX is like All, but panics if an error occurs.
func (mvq *MathVersionQuery) AllX(ctx context.Context) []*MathVersion {
	nodes, err := mvq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of MathVersion IDs.
func (mvq *MathVersionQuery) IDs(ctx context.Context) (ids []int, err error) {
	if mvq.ctx.Unique == nil && mvq.path != nil {
		mvq.Unique(true)
	}
	ctx = setContextOp(ctx, mvq.ctx, ent.OpQueryIDs)
	if err = mvq.Select(mathversion.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (mvq *MathVersionQuery) IDsX(ctx context.Context) []int {
	ids, err := mvq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (mvq *MathVersionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, mvq.ctx, ent.OpQueryCount)
	if err := mvq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, mvq, querierCount[*MathVersionQuery](), mvq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (mvq *MathVersionQuery) CountX(ctx context.Context) int {
	count, err := mvq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (mvq *MathVersionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, mvq.ctx, ent.OpQueryExist)
	switch _, err := mvq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (mvq *MathVersionQuery) ExistX(ctx context.Context) bool {
	exist, err := mvq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the MathVersionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (mvq *MathVersionQuery) Clone() *MathVersionQuery {
	if mvq == nil {
		return nil
	}
	return &MathVersionQuery{
		config:          mvq.config,
		ctx:             mvq.ctx.Clone(),
		order:           append([]mathversion.OrderOption{}, mvq.order...),
		inters:          append([]Interceptor{}, mvq.inters...),
		predicates:      append([]predicate.MathVersion{}, mvq.predicates...),
		withSessions:    mvq.withSessions.Clone(),
		withGameConfigs: mvq.withGameConfigs.Clone(),
		// clone intermediate query.
		sql:  mvq.sql.Clone(),
		path: mvq.path,
	}
}

// WithSessions tells the query-builder to eager-load the nodes that are connected to
// the "sessions" edge. The optional arguments are used to configure the query builder of the edge.
func (mvq *MathVersionQuery) WithSessions(opts ...func(*SessionQuery)) *MathVersionQuery {
	query := (&SessionClient{config: mvq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mvq.withSessions = query
	return mvq
}

// WithGameConfigs tells the query-builder to eager-load the nodes that are connected to
// the "game_configs" edge. The optional arguments are used to configure the query builder of the edge.
func (mvq *MathVersionQuery) WithGameConfigs(opts ...func(*GameConfigQuery)) *MathVersionQuery {
	query := (&GameConfigClient{config: mvq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	mvq.withGameConfigs = query
	return mvq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.MathVersion.Query().
//		GroupBy(mathversion.FieldName).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (mvq *MathVersionQuery) GroupBy(field string, fields ...string) *MathVersionGroupBy {
	mvq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &MathVersionGroupBy{build: mvq}
	grbuild.flds = &mvq.ctx.Fields
	grbuild.label = mathversion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Name string `json:"name,omitempty"`
//	}
//
//	client.MathVersion.Query().
//		Select(mathversion.FieldName).
//		Scan(ctx, &v)
func (mvq *MathVersionQuery) Select(fields ...string) *MathVersionSelect {
	mvq.ctx.Fields = append(mvq.ctx.Fields, fields...)
	sbuild := &MathVersionSelect{MathVersionQuery: mvq}
	sbuild.label = mathversion.Label
	sbuild.flds, sbuild.scan = &mvq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a MathVersionSelect configured with the given aggregations.
func (mvq *MathVersionQuery) Aggregate(fns ...AggregateFunc) *MathVersionSelect {
	return mvq.Select().Aggregate(fns...)
}

func (mvq *MathVersionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range mvq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, mvq); err != nil {
				return err
			}
		}
	}
	for _, f := range mvq.ctx.Fields {
		if !mathversion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if mvq.path != nil {
		prev, err := mvq.path(ctx)
		if err != nil {
			return err
		}
		mvq.sql = prev
	}
	return nil
}

func (mvq *MathVersionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*MathVersion, error) {
	var (
		nodes       = []*MathVersion{}
		_spec       = mvq.querySpec()
		loadedTypes = [2]bool{
			mvq.withSessions != nil,
			mvq.withGameConfigs != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*MathVersion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &MathVersion{config: mvq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, mvq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := mvq.withSessions; query != nil {
		if err := mvq.loadSessions(ctx, query, nodes,
			func(n *MathVersion) { n.Edges.Sessions = []*Session{} },
			func(n *MathVersion, e *Session) { n.Edges.Sessions = append(n.Edges.Sessions, e) }); err != nil {
			return nil, err
		}
	}
	if query := mvq.withGameConfigs; query != nil {
		if err := mvq.loadGameConfigs(ctx, query, nodes,
			func(n *MathVersion) { n.Edges.GameConfigs = []*GameConfig{} },
			func(n *MathVersion, e *GameConfig) { n.Edges.GameConfigs = append(n.Edges.GameConfigs, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (mvq *MathVersionQuery) loadSessions(ctx context.Context, query *SessionQuery, nodes []*MathVersion, init func(*MathVersion), assign func(*MathVersion, *Session)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*MathVersion)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.Session(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(mathversion.SessionsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.math_version_sessions
		if fk == nil {
			return fmt.Errorf(`foreign-key "math_version_sessions" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "math_version_sessions" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (mvq *MathVersionQuery) loadGameConfigs(ctx context.Context, query *GameConfigQuery, nodes []*MathVersion, init func(*MathVersion), assign func(*MathVersion, *GameConfig)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[int]*MathVersion)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.GameConfig(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(mathversion.GameConfigsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.math_version_game_configs
		if fk == nil {
			return fmt.Errorf(`foreign-key "math_version_game_configs" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "math_version_game_configs" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (mvq *MathVersionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := mvq.querySpec()
	_spec.Node.Columns = mvq.ctx.Fields
	if len(mvq.ctx.Fields) > 0 {
		_spec.Unique = mvq.ctx.Unique != nil && *mvq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, mvq.driver, _spec)
}

func (mvq *MathVersionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(mathversion.Table, mathversion.Columns, sqlgraph.NewFieldSpec(mathversion.FieldID, field.TypeInt))
	_spec.From = mvq.sql
	if unique := mvq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if mvq.path != nil {
		_spec.Unique = true
	}
	if fields := mvq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, mathversion.FieldID)
		for i := range fields {
			if fields[i] != mathversion.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := mvq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := mvq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := mvq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := mvq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (mvq *MathVersionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(mvq.driver.Dialect())
	t1 := builder.Table(mathversion.Table)
	columns := mvq.ctx.Fields
	if len(columns) == 0 {
		columns = mathversion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if mvq.sql != nil {
		selector = mvq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if mvq.ctx.Unique != nil && *mvq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range mvq.predicates {
		p(selector)
	}
	for _, p := range mvq.order {
		p(selector)
	}
	if offset := mvq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := mvq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// MathVersionGroupBy is the group-by builder for MathVersion entities.
type MathVersionGroupBy struct {
	selector
	build *MathVersionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (mvgb *MathVersionGroupBy) Aggregate(fns ...AggregateFunc) *MathVersionGroupBy {
	mvgb.fns = append(mvgb.fns, fns...)
	return mvgb
}

// Scan applies the selector query and scans the result into the given value.
func (mvgb *MathVersionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mvgb.build.ctx, ent.OpQueryGroupBy)
	if err := mvgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MathVersionQuery, *MathVersionGroupBy](ctx, mvgb.build, mvgb, mvgb.build.inters, v)
}

func (mvgb *MathVersionGroupBy) sqlScan(ctx context.Context, root *MathVersionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(mvgb.fns))
	for _, fn := range mvgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*mvgb.flds)+len(mvgb.fns))
		for _, f := range *mvgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*mvgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mvgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// MathVersionSelect is the builder for selecting fields of MathVersion entities.
type MathVersionSelect struct {
	*MathVersionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (mvs *MathVersionSelect) Aggregate(fns ...AggregateFunc) *MathVersionSelect {
	mvs.fns = append(mvs.fns, fns...)
	return mvs
}

// Scan applies the selector query and scans the result into the given value.
func (mvs *MathVersionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, mvs.ctx, ent.OpQuerySelect)
	if err := mvs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*MathVersionQuery, *MathVersionSelect](ctx, mvs.MathVersionQuery, mvs, mvs.inters, v)
}

func (mvs *MathVersionSelect) sqlScan(ctx context.Context, root *MathVersionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(mvs.fns))
	for _, fn := range mvs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*mvs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := mvs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
